[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app fallback so any deep link resolves to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
